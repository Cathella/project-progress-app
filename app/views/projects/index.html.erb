<div class="py-5">
  <div class="row">
    <div class="col-lg-8 mx-auto">
      <h1 class="fw-bold border-bottom pb-2">My Project</h1>

      <div class="mt-3">
        <% @projects.each do |project| %>
          <p class="bg-light py-1 px-2"><%= link_to project.name, project %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>